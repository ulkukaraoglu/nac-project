# Postgres
POSTGRES_USER= ${POSTGRES_USER}
POSTGRES_DB= ${POSTGRES_DB}
POSTGRES_PASSWORD= ${POSTGRES_PASSWORD}
POSTGRES_PORT= ${POSTGRES_PORT}


# Redis
REDIS_PORT= ${REDIS_PORT}

# FreeRADIUS
RADIUS_AUTH_PORT= ${RADIUS_AUTH_PORT}
RADIUS_ACCOUNTING_PORT= ${RADIUS_ACCOUNTING_PORT}


# API
API_ENV=local
API_PORT= ${API_PORT}

# Auth rate limiting 
AUTH_RATE_MAX_ATTEMPTS= ${AUTH_RATE_MAX_ATTEMPTS}
AUTH_RATE_WINDOW_SECONDS= ${AUTH_RATE_WINDOW_SECONDS}
AUTH_RATE_BLOCK_SECONDS= ${AUTH_RATE_BLOCK_SECONDS}

